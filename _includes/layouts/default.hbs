<!DOCTYPE html>
<html lang="en" data-theme="light">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="color-scheme" content="dark light" />
    <!-- SEO Meta -->
    <meta name="description"
          content="{{#if description}}{{ description }}{{ else }}Interactive browser-based tool for analyzing anthropometric data from ANSUR surveys. Explore body measurement distributions, correlations, and statistical models.{{/if}}" />
    <meta name="keywords"
          content="anthropometry, ANSUR, body measurements, anthropometric data, statistics, data visualization" />
    <meta name="author" content="patchoulish" />
    <meta name="theme-color" content="#13171f" />
    <!-- Open Graph Meta -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content="{{ title }}" />
    <meta property="og:description"
          content="{{#if description}}{{ description }}{{ else }}Interactive browser-based tool for analyzing anthropometric data from ANSUR surveys. Explore body measurement distributions, correlations, and statistical models.{{/if}}" />
    <meta property="og:url"
          content="{{#if canonicalUrl}}{{ canonicalUrl }}{{ else }}https://anthro.patchouli.sh/{{/if}}" />
    <meta property="og:site_name" content="Anthropometry Playground" />
    <!-- Twitter Card Meta -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="{{ title }}" />
    <meta name="twitter:description"
          content="{{#if description}}{{ description }}{{ else }}Interactive browser-based tool for analyzing anthropometric data from ANSUR surveys. Explore body measurement distributions, correlations, and statistical models.{{/if}}" />
    <!-- Page Title -->
    <title>{{ title }}</title>
    <!-- Canonical URL -->
    <link rel="canonical"
          href="{{#if canonicalUrl}}{{ canonicalUrl }}{{ else }}https://anthro.patchouli.sh/{{/if}}" />
    <!-- Favicon and App Icons -->
    <link rel="icon" href="/favicon.ico" sizes="32x32" />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <!-- Stylesheets -->
    <link rel="stylesheet" href="assets/styles/theme-toggle.css" />
    <link rel="stylesheet" href="assets/styles/main.css" />
    <!-- Structured Data (JSON-LD) -->
    {{> seo-structured-data }}
  </head>
  <body class="container">
    {{> header }}
    <main>
      {{{ content }}}
    </main>
    {{> footer }}
    <script src="assets/scripts/theme-toggle.js"></script>
    <script type="module" src="assets/scripts/main.js"></script>
  </body>
</html>
